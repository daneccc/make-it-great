import Foundation
import SwiftUI

enum Theme {
    static let secondary = Color("YellowMonDay")
    static let tertiary = Color("RedMonDay")
    static let action = Color("BlueMonDay")
    static let font = Color("FontMonDay")
}
